CREATE TABLE GRADE(
	GRADE_NUM INT PRIMARY KEY,				--등급별 PK
	GRADE_PROFILE VARCHAR(2048) NOT NULL,	--등급 이미지 URL
	GRADE_NAME VARCHAR(100) NOT NULL,		--등급에 대한 이름
	GRADE_MIN_POINT INT NOT NULL,			--해당 등급에 최소 포인트 범위
	GRADE_MAX_POINT INT NOT NULL			--해당 등급에 최대 포인트 범위
);

ALTER TABLE GRADE
RENAME COLUMN  GREAD_MAX_POINT TO GRADE_MAX_POINT;


--MAX POINT ASC SELECT ALL

-- 등급 출력
SELECT
	GRADE_NUM,
	GRADE_PROFILE,
	GRADE_NAME,
	GRADE_MIN_POINT,
	GRADE_MAX_POINT
FROM
	GRADE
WHERE
	GRADE_NUM = ?
	
--높은등급순으로 정렬
SELECT
	GRADE_NUM,
	GRADE_PROFILE,
	GRADE_NAME,
	GRADE_MIN_POINT,
	GRADE_MAX_POINT
FROM
	GRADE
ORDER BY
	GRADE_MAX_POINT DESC

--insert
INSERT INTO GRADE(GRADE_NUM,GRADE_PROFILE,GRADE_NAME,GRADE_MIN_POINT,GRADE_MAX_POINT)
VALUES((SELECT NVL(MAX(GRADE_NUM),0)+1 FROM GRADE),'default.jpg','sample name1',0,100);

INSERT INTO GRADE (GRADE_NUM, GRADE_PROFILE, GRADE_NAME, GRADE_MIN_POINT, GRADE_MAX_POINT)
SELECT NVL(MAX(GRADE_NUM), 0) + 1, 'default.jpg', 'sample name1', 0, 100
FROM GRADE;


SELECT * FROM GRADE;

SELECT COLUMN_NAME
FROM ALL_TAB_COLUMNS
WHERE TABLE_NAME = 'GRADE';

